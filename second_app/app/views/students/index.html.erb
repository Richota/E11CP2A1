<p id="notice"><%= notice %></p>

<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th class="separacion">Age</th>
      <th>eMail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <div class="well">
        <tr>
          <td><%= student.name %></td>
          <td class="separacion"><%= student.age %></td>
          <td><%= student.email %></td>
          <td><%= link_to 'Show', student, class: 'btn btn-secondary' %></td>
          <td><%= link_to 'Edit', edit_student_path(student), class: 'btn btn-info' %></td>
          <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Estás seguro?' }, class: 'btn btn-warning' %></td>
        </tr>
      </div>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Student', new_student_path %>
